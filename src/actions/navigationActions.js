import * as types from '../constants/actionTypes';

export const toggleMenu = () => ({
  type: types.NAVIGATION_TOGGLE_MENU
});

export const toggleAuth = () => ({
  type: types.NAVIGATION_TOGGLE_AUTH
});
